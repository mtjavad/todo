{
  "name": "mobapp",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mtjavad.github.io/todo",
  "dependencies": {
    "babel-plugin-styled-components": "^1.9.3",
    "gh-pages": "^2.0.1",
    "global": "^4.3.2",
    "mobx": "^5.7.0",
    "mobx-react": "^5.4.2",
    "react": "^16.6.3",
    "react-app-rewire-mobx": "^1.0.9",
    "react-dom": "^16.6.3",
    "react-scripts": "^1.1.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom"
  },
  "babel": {
    "plugins": [
      "@babel/plugin-proposal-decorators"
    ],
    "presets": [
      "react-app"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "react-app-rewired": "^1.6.2"
  }
}
